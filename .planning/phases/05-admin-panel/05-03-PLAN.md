---
phase: 05-admin-panel
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - app/admin/bans.server.ts
  - app/admin/mutes.server.ts
  - app/routes/admin._index.tsx
autonomous: true
requirements: [ADMN-03, ADMN-04]
user_setup: []

must_haves:
  truths:
    - "Admin can view, add, and unban (sa_bans + sa_unbans)"
    - "Admin can view, add, and unmute (sa_mutes + sa_unmutes)"
  artifacts:
    - path: app/admin/bans.server.ts
      provides: "listBans(), addBan(), unban() using sa_bans and sa_unbans; getMySQLPool"
    - path: app/admin/mutes.server.ts
      provides: "listMutes(), addMute(), unmute() using sa_mutes and sa_unmutes; getMySQLPool"
    - path: app/routes/admin._index.tsx
      provides: "Bans and Mutes sections with list, add form, remove action"
  key_links:
    - from: app/admin/bans.server.ts
      to: app/mysql.server.ts
      via: "getMySQLPool()"
      pattern: "getMySQLPool"
    - from: app/admin/mutes.server.ts
      to: app/mysql.server.ts
      via: "getMySQLPool()"
      pattern: "getMySQLPool"
---

<objective>
Implement bans and mutes using tables sa_bans, sa_mutes, sa_unbans, sa_unmutes. See EXISTING_MYSQL_SCHEMA.md. listBans/listMutes: SELECT from sa_bans/sa_mutes (status ACTIVE or all). addBan: INSERT sa_bans (player_name, player_steamid, player_ip?, admin_steamid, admin_name, reason, duration, ends, status). unban(ban_id, admin_id, reason): INSERT sa_unbans, UPDATE sa_bans SET status='UNBANNED', unban_id. addMute/unmute: same pattern for sa_mutes/sa_unmutes. admin_id for unban/unmute comes from sa_admins (caller passes or resolve by current user).
</objective>

<execution_context>
@C:/Users/Merih/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Merih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-panel/EXISTING_MYSQL_SCHEMA.md

# sa_bans, sa_mutes, sa_unbans, sa_unmutes
- Mevcut MySQL veritabanındaki ban ve mute tablolarını kullan; yeni tablo oluşturulmaz. Sütun isimleri mevcut şemaya göre (örn. id, identifier, reason, expires_at, created_at veya DB’deki gerçek isimler) yazılacak.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bans and mutes server modules (mevcut MySQL tabloları)</name>
  <files>app/admin/bans.server.ts, app/admin/mutes.server.ts</files>
  <action>
Mevcut MySQL’deki ban ve mute tablolarını kullan (yeni şema dokümanı veya CREATE TABLE yok). Tablo ve sütun isimleri mevcut veritabanına uyacak; standart isimler varsay: ban tablosu (id, identifier, reason, expires_at, created_at), mute tablosu aynı yapı. Gerçek şema farklıysa kodda yorumla belirt veya sütun eşlemesi yap.

Create app/admin/bans.server.ts (server-only). Export listBans(): getMySQLPool(); if null return []. SELECT from sa_bans ORDER BY created DESC. Return typed array. addBan({ identifier, reason, expires_at }): INSERT; return { ok: true } or { ok: false, error }. removeBan(id): DELETE WHERE id = ?. Parameterized queries.

Create app/admin/mutes.server.ts: listMutes(), addMute({ identifier, reason, expires_at }), removeMute(id). Same pattern; table sa_mutes.
  </action>
  <verify>
    <automated>grep -q "listBans\|addBan\|unban" app/admin/bans.server.ts && grep -q "listMutes\|addMute\|unmute" app/admin/mutes.server.ts</automated>
  </verify>
  <done>bans.server.ts and mutes.server.ts: list/add/unban (sa_bans, sa_unbans), list/add/unmute (sa_mutes, sa_unmutes). Column names and unban/unmute flow in EXISTING_MYSQL_SCHEMA.md.</done>
</task>

<task type="auto">
  <name>Task 2: Admin Bans and Mutes sections UI</name>
  <files>app/routes/admin._index.tsx</files>
  <action>
Loader: listBans(), listMutes(); add to loader data. Action: "add-ban" → addBan(player_name, player_steamid, admin_steamid, admin_name, reason, duration, ends?); "unban" → unban(banId, adminId, reason). "add-mute" → addMute(...); "unmute" → unmute(muteId, adminId, reason). adminId: from session (current user) lookup sa_admins.id or pass 0 if not required.

Render Bans: table (player_name, player_steamid, reason, duration, ends, status, created); Add form (player_name, player_steamid, player_ip?, admin_steamid, admin_name, reason, duration, ends); Unban button per row. Mutes: same (type GAG/MUTE/SILENCE). Tailwind; errors inline.
  </action>
  <verify>
    <automated>grep -q "listBans\|listMutes\|bans\|mutes" app/routes/admin._index.tsx</automated>
  </verify>
  <done>Admin can manage bans and mutes from admin panel (view, add, remove).</done>
</task>

</tasks>

<verification>
- Admin can list/add/unban bans (sa_bans, sa_unbans); list/add/unmute mutes (sa_mutes, sa_unmutes).
</verification>

<success_criteria>
- ADMN-03 and ADMN-04 satisfied: Admin can manage bans; admin can manage mutes.
</success_criteria>

<output>
After completion, create .planning/phases/05-admin-panel/05-03-SUMMARY.md
</output>
