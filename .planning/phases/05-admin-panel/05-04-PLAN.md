---
phase: 05-admin-panel
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - app/admin/servers.server.ts
  - app/routes/admin._index.tsx
  - app/routes/_index.tsx
autonomous: true
requirements: [ADMN-05]
user_setup: []

must_haves:
  truths:
    - "Admin can view server list (mevcut MySQL server tablosundan)"
    - "Admin can add, edit, and remove server entries (MySQL’e yazılır)"
    - "Homepage server listesini MySQL’den okur; boşsa app/data/servers.ts SERVER_LIST fallback"
  artifacts:
    - path: app/admin/servers.server.ts
      provides: "listServers(), getServerListForDisplay(), addServer(), updateServer(), removeServer(); mevcut MySQL şeması"
    - path: app/routes/_index.tsx
      provides: "Loader uses getServerListForDisplay() (MySQL veya SERVER_LIST fallback)"
  key_links:
    - from: app/routes/_index.tsx
      to: app/admin/servers.server.ts or getServerListForDisplay()
      via: "Loader calls shared helper that returns server entries"
      pattern: "server_list|getServerList"
    - from: app/routes/admin._index.tsx
      to: app/admin/servers.server.ts
      via: "listServers, addServer, updateServer, removeServer"
      pattern: "listServers|addServer"
---

<objective>
Admin server bilgisini görüntülesin ve düzenlesin: mevcut MySQL’deki server listesi tablosunu kullan (yeni şema/migration yok). getServerListForDisplay() MySQL’den okur; boşsa app/data/servers.ts SERVER_LIST fallback. Admin panel Servers bölümü listeleme, ekleme, düzenleme, silme. Tablo/sütun isimleri mevcut veritabanına uyacak.
</objective>

<execution_context>
@C:/Users/Merih/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Merih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-panel/5-CONTEXT.md
@.planning/phases/04-buy-vip/VIP_TABLE_SCHEMA.md

# Server list
- Mevcut MySQL’de server listesi tablosu var; yeni tablo oluşturulmaz. app/data/servers.ts SERVER_LIST fallback olarak kalır. getServerListForDisplay() önce MySQL’den okur; yoksa/boşsa SERVER_LIST döner.
</context>

<interfaces>
From app/data/servers.ts:
- ServerEntry: { host: string; port?: number; gamemode?: string }
- SERVER_LIST: ServerEntry[]
From app/routes/_index.tsx loader: uses SERVER_LIST.map(...) for gamedig; expects entries with host, port, gamemode.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: servers.server.ts (mevcut MySQL server tablosu)</name>
  <files>app/admin/servers.server.ts</files>
  <action>
Mevcut MySQL’deki server listesi tablosunu kullan (yeni şema dokümanı veya CREATE TABLE yok). Tablo/sütun isimleri mevcut veritabanına uyacak (örn. id, host, port, gamemode, sort_order veya DB’deki gerçek isimler).

Create app/admin/servers.server.ts (server-only). Export:
- listServers(): getMySQLPool(); if null return []. SELECT from server table ORDER BY sort_order ASC, id ASC. Return array of { id, host, port, gamemode, sort_order } (or existing columns).
- getServerListForDisplay(): Promise&lt;Array&lt;{ host: string; port?: number; gamemode?: string }&gt;&gt; — if pool null, return SERVER_LIST from ~/data/servers. Else SELECT from server table; if 0 rows or error, return SERVER_LIST. Else map rows to { host, port, gamemode } for homepage.
- addServer({ host, port?, gamemode? }): INSERT; return { ok: true, id } or { ok: false, error }.
- updateServer(id, { host?, port?, gamemode?, sort_order? }): UPDATE WHERE id = ?. Return { ok: true } or { ok: false, error }.
- removeServer(id): DELETE WHERE id = ?. Return { ok: true } or { ok: false, error }.

Use getMySQLPool(); handle null and errors.
  </action>
  <verify>
    <automated>grep -q "getServerListForDisplay\|listServers" app/admin/servers.server.ts</automated>
  </verify>
  <done>servers.server.ts with listServers, getServerListForDisplay, addServer, updateServer, removeServer against existing MySQL table.</done>
</task>

<task type="auto">
  <name>Task 2: Homepage loader uses getServerListForDisplay; admin Servers section</name>
  <files>app/routes/_index.tsx, app/routes/admin._index.tsx</files>
  <action>
In app/routes/_index.tsx loader: replace use of SERVER_LIST with getServerListForDisplay() from ~/admin/servers.server. So: const serverEntries = await getServerListForDisplay(); then run gamedig over serverEntries (same as current SERVER_LIST.map). No change to component; only loader data source. When MySQL has no table or no rows, getServerListForDisplay returns SERVER_LIST from servers.ts so behavior is unchanged.

In app/routes/admin._index.tsx: loader calls listServers() and includes servers in loader data. Action: handle "add-server" (host, port, gamemode), "update-server" (id, host?, port?, gamemode?, sort_order?), "remove-server" (id). Call addServer, updateServer, removeServer. Render Servers section: table of servers (host, port, gamemode, sort_order); Add form (host, port, gamemode); per-row Edit (inline or modal) and Remove. Style with Tailwind; match VIP/Bans/Mutes sections. If listServers returns [] and MySQL is configured, show empty state with "Add server" only; if MySQL not configured, listServers is [] and getServerListForDisplay still feeds homepage from SERVER_LIST — admin can be told "Add MySQL server_list to persist" or show read-only SERVER_LIST for view.
  </action>
  <verify>
    <automated>grep -q "getServerListForDisplay" app/routes/_index.tsx && grep -q "listServers\|servers" app/routes/admin._index.tsx</automated>
  </verify>
  <done>Homepage uses getServerListForDisplay(); admin can view and edit server list (add/update/remove) persisted in MySQL.</done>
</task>

</tasks>

<verification>
- Homepage still works when server_list empty or missing (fallback to SERVER_LIST). Admin can add/edit/remove servers; homepage reflects MySQL list when populated.
</verification>

<success_criteria>
- ADMN-05 satisfied: Admin can view and edit server info; homepage displays from MySQL when available.
</success_criteria>

<output>
After completion, create .planning/phases/05-admin-panel/05-04-SUMMARY.md
</output>
