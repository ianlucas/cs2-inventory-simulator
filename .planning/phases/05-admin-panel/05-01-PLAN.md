---
phase: 05-admin-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/auth.server.ts
  - app/responses.server.ts
  - app/routes/admin.tsx
  - app/routes/admin._index.tsx
  - .env.example
autonomous: true
requirements: [ADMN-01]
user_setup: []

must_haves:
  truths:
    - "Only users with admin flag can access /admin routes"
    - "Non-admin authenticated user receives 403 when accessing /admin"
    - "Unauthenticated user is redirected to sign-in when accessing /admin"
  artifacts:
    - path: app/auth.server.ts
      provides: "isAdmin(request) using getRequestUserId + ADMIN_STEAM_IDS env"
    - path: app/routes/admin.tsx
      provides: "Layout loader that requireUser then isAdmin or 403"
    - path: app/routes/admin._index.tsx
      provides: "Admin dashboard with nav/sections for VIP, Bans, Mutes, Servers"
  key_links:
    - from: app/routes/admin.tsx
      to: app/auth.server.ts
      via: "requireUser(request), isAdmin(request)"
      pattern: "requireUser|isAdmin"
---

<objective>
Gate the admin panel with existing Steam auth and an admin flag. Add isAdmin(request) using env ADMIN_STEAM_IDS; admin layout returns 403 for non-admin; admin index page with navigation to VIP, Bans, Mutes, Server info sections.
</objective>

<execution_context>
@C:/Users/Merih/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Merih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-panel/5-CONTEXT.md

# Auth and routes
- getRequestUserId(request) and requireUser(request) in app/auth.server.ts; session holds userId (Steam ID).
- Admin flag: use env ADMIN_STEAM_IDS (comma-separated Steam IDs). No Prisma/Group change required.
- File-based routes: admin.tsx = layout for /admin/*; admin._index.tsx = /admin index. Loader in layout runs first.
</context>

<interfaces>
From app/auth.server.ts:
- getRequestUserId(request: Request): Promise<string | undefined>
- requireUser(request: Request): Promise<User> — redirects to /sign-in if no user
- findRequestUser(request: Request): Promise<User | undefined>

From app/responses.server.ts:
- unauthorized, notFound, methodNotAllowed, etc. Add forbidden (403) for admin gate.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Admin check and 403 response</name>
  <files>app/auth.server.ts, app/responses.server.ts, .env.example</files>
  <action>
In app/responses.server.ts add: export const forbidden = new Response(null, { status: 403 });

In app/auth.server.ts add isAdmin(request: Request): Promise&lt;boolean&gt;. Implementation: const userId = await getRequestUserId(request); if (!userId) return false; const ids = process.env.ADMIN_STEAM_IDS ?? ""; const list = ids.split(",").map(s => s.trim()).filter(Boolean); return list.includes(userId); Export isAdmin.

In .env.example add commented block:
# Admin panel (optional; comma-separated Steam IDs)
# ADMIN_STEAM_IDS=76561198000000000,76561198000000001

Do not add ADMIN_STEAM_IDS to env.server.ts assert — optional; app runs without it (no admins).
  </action>
  <verify>
    <automated>grep -q "isAdmin" app/auth.server.ts && grep -q "forbidden" app/responses.server.ts && grep -q "ADMIN_STEAM_IDS" .env.example</automated>
  </verify>
  <done>isAdmin(request) and forbidden response exist; .env.example documents ADMIN_STEAM_IDS.</done>
</task>

<task type="auto">
  <name>Task 2: Admin layout and index route</name>
  <files>app/routes/admin.tsx, app/routes/admin._index.tsx</files>
  <action>
Create app/routes/admin.tsx as layout route. Export loader: await requireUser(request); if (!(await isAdmin(request))) throw forbidden; return {}; Export Component: Outlet. Use middleware(request) if other layout routes do (see app/routes/_index.tsx). Ensure layout loader runs for all /admin and child routes.

Create app/routes/admin._index.tsx. Loader: return {} (or minimal data). Component: render admin dashboard with navigation to four sections: VIP, Bans, Mutes, Server info. Use React Router Link or in-page anchors/sections so later plans can add content under same page or child routes. Prefer single page with section ids (e.g. #vips, #bans, #mutes, #servers) or tabs; style with Tailwind to match app. Title/meta: "Admin" or similar. Do not implement VIP/Bans/Mutes/Servers CRUD here — placeholder sections or links only.
  </action>
  <verify>
    <automated>grep -q "isAdmin\|forbidden" app/routes/admin.tsx && test -f app/routes/admin._index.tsx</automated>
  </verify>
  <done>Visiting /admin as admin shows dashboard with nav; as non-admin returns 403; unauthenticated redirects to sign-in.</done>
</task>

</tasks>

<verification>
- Run app; set ADMIN_STEAM_IDS to current user's Steam ID; visit /admin — dashboard visible.
- Remove admin ID or use other user — 403.
- Sign out, visit /admin — redirect to sign-in.
</verification>

<success_criteria>
- ADMN-01 satisfied: Admin can access admin panel using existing Steam auth and admin flag; non-admin gets 403.
</success_criteria>

<output>
After completion, create .planning/phases/05-admin-panel/05-01-SUMMARY.md
</output>
