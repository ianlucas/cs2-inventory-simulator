---
phase: 05-admin-panel
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - app/routes/admin._index.tsx
  - app/admin/vip.server.ts
autonomous: true
requirements: [ADMN-02]
user_setup: []

must_haves:
  truths:
    - "Admin can view list of VIP entries from MySQL vip_users table"
    - "Admin can add a VIP entry (account_id, name, sid, group, expires)"
    - "Admin can remove a VIP entry (by account_id + sid)"
  artifacts:
    - path: app/admin/vip.server.ts
      provides: "listVips(), addVip(), removeVip() using getMySQLPool and vip_users table"
    - path: app/routes/admin._index.tsx
      provides: "VIP section: table/list, add form, remove action"
  key_links:
    - from: app/admin/vip.server.ts
      to: app/mysql.server.ts
      via: "getMySQLPool()"
      pattern: "getMySQLPool"
    - from: app/routes/admin._index.tsx
      to: app/admin/vip.server.ts
      via: "loader + action calling listVips/addVip/removeVip"
      pattern: "listVips|addVip|removeVip"
---

<objective>
Implement VIP management in the admin panel using table vip_users. Columns: account_id (BIGINT), name (VARCHAR(64)), lastvisit (BIGINT), sid (BIGINT), group (VARCHAR(64)), expires (BIGINT). PK (account_id, sid). listVips: SELECT from vip_users. addVip: INSERT with account_id, name, lastvisit, sid, group, expires. removeVip: DELETE WHERE account_id = ? AND sid = ?. See EXISTING_MYSQL_SCHEMA.md.
</objective>

<execution_context>
@C:/Users/Merih/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Merih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-admin-panel/EXISTING_MYSQL_SCHEMA.md

# vip_users (MySQL)
- Tablo: vip_users. Sütunlar: account_id (BIGINT PK), name (VARCHAR(64)), lastvisit (BIGINT), sid (BIGINT PK), group (VARCHAR(64)), expires (BIGINT). Composite PK (account_id, sid). getMySQLPool() in app/mysql.server.ts.
</context>

<tasks>

<task type="auto">
  <name>Task 1: VIP server module (list, add, remove)</name>
  <files>app/admin/vip.server.ts</files>
  <action>
Create app/admin/vip.server.ts (server-only). Table: vip_users. Export:

- listVips(): getMySQLPool(); if null return []. SELECT account_id, name, lastvisit, sid, group, expires FROM vip_users ORDER BY expires DESC. Return typed array (account_id, name, lastvisit, sid, group, expires).

- addVip(params: { account_id: number; name: string; lastvisit: number; sid: number; group: string; expires: number }): getMySQLPool(); if null return { ok: false, error }. INSERT INTO vip_users (account_id, name, lastvisit, sid, group, expires) VALUES (?, ?, ?, ?, ?, ?). Return { ok: true } or { ok: false, error }.

- removeVip(account_id: number, sid: number): DELETE FROM vip_users WHERE account_id = ? AND sid = ?. Return { ok: true } or { ok: false, error }.

Use mysql2/promise; parameterized queries.
  </action>
  <verify>
    <automated>grep -q "listVips\|addVip\|removeVip" app/admin/vip.server.ts && grep -q "getMySQLPool" app/admin/vip.server.ts</automated>
  </verify>
  <done>listVips, addVip, removeVip implemented and exported; use getMySQLPool.</done>
</task>

<task type="auto">
  <name>Task 2: Admin VIP section UI and actions</name>
  <files>app/routes/admin._index.tsx</files>
  <action>
In app/routes/admin._index.tsx: loader calls listVips(), include vips in data. Action: intent "add-vip" → account_id, name, lastvisit, sid, group, expires; call addVip(). intent "remove-vip" → account_id, sid; call removeVip(account_id, sid).

Render VIP section: table of vip_users (account_id, name, lastvisit, sid, group, expires); "Add VIP" form (account_id, name, lastvisit, sid, group, expires); per-row "Remove" (account_id + sid). Tailwind; errors inline or flash.
  </action>
  <verify>
    <automated>grep -l "listVips\|vips" app/routes/admin._index.tsx && (grep -q "addVip\|removeVip\|add-vip\|remove-vip" app/routes/admin._index.tsx)</automated>
  </verify>
  <done>Admin can view VIP list, add a VIP, and remove a VIP from the admin panel.</done>
</task>

</tasks>

<verification>
- As admin, open /admin; VIP section shows rows from vip table; add form inserts new row; remove deletes row.
</verification>

<success_criteria>
- ADMN-02 satisfied: Admin can manage VIPs (view, add, remove).
</success_criteria>

<output>
After completion, create .planning/phases/05-admin-panel/05-02-SUMMARY.md
</output>
