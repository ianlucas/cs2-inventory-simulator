---
phase: 01-data-dual-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/env.server.ts
  - .env.example
  - package.json
  - app/mysql.server.ts
  - app/mysql.server.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
user_setup: []

must_haves:
  truths:
    - "App can obtain a MySQL pool when MYSQL_URL is set"
    - "App starts without MYSQL_URL (optional; hub can be disabled)"
    - "Existing PostgreSQL usage (db.server.ts, Prisma) is unchanged"
    - "No module mixes Prisma and MySQL client"
  artifacts:
    - path: app/env.server.ts
      provides: "MYSQL_URL optional read"
      contains: "MYSQL_URL"
    - path: app/mysql.server.ts
      provides: "MySQL pool singleton getter"
      exports: ["getMySQLPool"]
    - path: .env.example
      provides: "MYSQL_URL documentation"
      contains: "MYSQL_URL"
    - path: app/mysql.server.test.ts
      provides: "Unit tests for getMySQLPool"
  key_links:
    - from: app/mysql.server.ts
      to: app/singleton.server.ts
      via: "singleton() for pool"
      pattern: "singleton\\(.*mysql"
    - from: app/mysql.server.ts
      to: "process.env.MYSQL_URL"
      via: "read in getter"
      pattern: "MYSQL_URL"
    - from: app/db.server.ts
      to: "no MySQL import"
      via: "unchanged"
      pattern: "prisma only"
---

<objective>
Establish the MySQL connection layer so the app can persist and read VIP, bans, and mutes from a separate MySQL database without touching PostgreSQL or Prisma.

Purpose: Satisfy Phase 1 success criteria — MySQL pool available when MYSQL_URL is set; PostgreSQL flows unchanged; no schema or connection mixing.
Output: Optional MYSQL_URL in env, app/mysql.server.ts with getMySQLPool(), unit tests, and proof that existing Postgres usage is unaffected.
</objective>

<execution_context>
@C:/Users/Merih/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Merih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-dual-database/1-CONTEXT.md
@.planning/phases/01-data-dual-database/1-RESEARCH.md

# Existing patterns to follow
- app/db.server.ts: singleton Prisma client, DATABASE_URL from env.server.ts, @prisma/adapter-pg. Do not modify this file.
- app/env.server.ts: assert() for required vars (DATABASE_URL, SESSION_SECRET); optional vars read without assert. Add MYSQL_URL as optional (no assert).
- app/singleton.server.ts: singleton(name, valueFactory). Reuse for MySQL pool.
- Server-only modules: *.server.ts; MySQL client must live in app/mysql.server.ts and only be imported from server context (loaders, actions).
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From app/env.server.ts (after change):
- MYSQL_URL will be read and exported optionally (e.g. export const MYSQL_URL = process.env.MYSQL_URL ?? undefined or read only in mysql.server.ts per RESEARCH).

From app/singleton.server.ts:
```typescript
export const singleton = <Value>(name: string, valueFactory: () => Value): Value
```

From 1-RESEARCH.md — getMySQLPool contract:
- Returns Pool | null (null when MYSQL_URL is unset).
- When MYSQL_URL is set, returns the same pool instance (singleton).
- Use mysql2/promise createPool(url); use singleton("mysql-pool", () => mysql.createPool(url)).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add MYSQL_URL and install mysql2</name>
  <files>app/env.server.ts, .env.example, package.json</files>
  <action>
Add optional MYSQL_URL support and the mysql2 dependency.

1. app/env.server.ts: Export MYSQL_URL without asserting. Use a simple read: `export const MYSQL_URL = process.env.MYSQL_URL ?? undefined` (or equivalent so it is string | undefined). Do not add assert() for MYSQL_URL — app must start when hub is disabled (per CONTEXT and RESEARCH).

2. .env.example: Add a commented or documented line for MYSQL_URL in the optional section, e.g. "# MySQL connection URL for hub (VIP, bans, mutes). Optional; omit if hub is disabled." and "MYSQL_URL=mysql://user:password@host:3306/dbname".

3. package.json: Add dependency "mysql2": "^3.x" (use current 3.x from npm). Run `npm install` to update package-lock.json.

Do not modify app/db.server.ts or any Prisma/PostgreSQL code.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>MYSQL_URL is exported from env.server.ts (optional); .env.example documents MYSQL_URL; mysql2 is in package.json and lockfile; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create MySQL pool module</name>
  <files>app/mysql.server.ts</files>
  <action>
Create a server-only MySQL pool module following RESEARCH and existing db.server.ts pattern.

1. Create app/mysql.server.ts. Import mysql from "mysql2/promise" and singleton from "~/singleton.server" (or the app's alias for app/). Import MYSQL_URL from "~/env.server" if you exported it in Task 1; otherwise read process.env.MYSQL_URL inside the getter.

2. Implement getMySQLPool(): When MYSQL_URL is absent (undefined or empty string), return null. When present, return singleton("mysql-pool", () => mysql.createPool(MYSQL_URL)). So the function returns Pool | null. Use createPool with the URL string only (no hand-rolled options per RESEARCH).

3. Export only getMySQLPool. Do not export the pool or mysql. File must be *.server.ts so it is never bundled for the client.

4. Do not import prisma or anything from app/db.server.ts in this file (no schema or connection mixing).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>app/mysql.server.ts exists; getMySQLPool() returns Pool | null; uses singleton and mysql2/promise createPool; no Prisma/Postgres imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add unit tests and verify Postgres untouched</name>
  <files>app/mysql.server.test.ts</files>
  <behavior>
    - When MYSQL_URL is unset (or undefined), getMySQLPool() returns null.
    - When MYSQL_URL is set, getMySQLPool() returns a non-null pool (same reference on repeated calls — singleton).
    - Existing test suite and typecheck still pass (no regressions to Postgres usage).
  </behavior>
  <action>
Add unit tests for getMySQLPool and confirm existing flows are unchanged.

1. Create app/mysql.server.test.ts. Use Vitest (describe/test, vi). Follow project pattern: test file next to source (see .planning/codebase/TESTING.md).

2. Test "returns null when MYSQL_URL is unset": Stub or clear process.env.MYSQL_URL (and avoid caching across tests — e.g. vi.stubEnv("MYSQL_URL", undefined) or save/restore env). Call getMySQLPool(); expect result to be null. Clear singleton state between tests if needed (e.g. by stubbing before first use) so the "unset" case is observable.

3. Test "returns pool when MYSQL_URL is set": vi.stubEnv("MYSQL_URL", "mysql://localhost:3306/test") (or a harmless URL). Call getMySQLPool() twice; expect both calls to return the same reference (singleton). You may need to clear global __singletons between tests if the test file runs "unset" and "set" in same process; document or use vi.resetModules() and re-import if necessary so "unset" test is reliable.

4. Do not require a real MySQL server for tests. Mocking the pool or testing only getter behavior is sufficient. Optional: if you can run a trivial pool.query("SELECT 1") when MYSQL_URL is set without a real DB (e.g. mock), add that; otherwise omit to keep tests fast and hermetic.

5. Run full test suite: npm test. Ensure no existing tests break (confirms DATA-02: PostgreSQL usage unchanged).
  </action>
  <verify>
    <automated>npm test -- app/mysql.server.test.ts</automated>
    <automated>npm test</automated>
  </verify>
  <done>app/mysql.server.test.ts exists; getMySQLPool() null when MYSQL_URL unset, singleton when set; full npm test passes.</done>
</task>

</tasks>

<verification>
- Run npm run typecheck and npm test. Both must pass.
- Confirm app/db.server.ts and prisma/schema.prisma were not modified.
- Confirm no file imports both prisma (or db.server) and getMySQLPool (or mysql.server).
</verification>

<success_criteria>
- MYSQL_URL is optional in env; app starts with or without it.
- getMySQLPool() returns Pool when MYSQL_URL set, null otherwise; singleton reuse.
- Unit tests cover null and singleton behavior; full suite green.
- PostgreSQL (db.server.ts, Prisma) unchanged and all existing tests pass.
</success_criteria>

<output>
After completion, create .planning/phases/01-data-dual-database/01-01-SUMMARY.md
</output>
