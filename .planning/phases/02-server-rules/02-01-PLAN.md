---
phase: 02-server-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/server-rules.ts
  - app/routes/rules._index.tsx
  - app/routes/rules._index.test.tsx
  - app/components/header.tsx
  - app/components/footer.tsx
  - app/translations/english.ts
autonomous: true
requirements:
  - RULES-01
user_setup: []

must_haves:
  truths:
    - User can open the server rules page from the app (header or footer)
    - Page at /rules displays server rules in sections with explanatory content
    - Rules content is static and public (no auth)
  artifacts:
    - path: app/data/server-rules.ts
      provides: Typed sections (title + body) for server rules
    - path: app/routes/rules._index.tsx
      provides: Route /rules with meta, loader, and page component
    - path: app/components/header.tsx
      provides: Header link to /rules
    - path: app/components/footer.tsx
      provides: Footer link to /rules
  key_links:
    - from: app/components/header.tsx
      to: /rules
      via: HeaderLink to="/rules"
      pattern: to="/rules"
    - from: app/components/footer.tsx
      to: /rules
      via: Link or <a href="/rules">
      pattern: /rules
    - from: app/routes/rules._index.tsx
      to: app/data/server-rules.ts
      via: import and render sections
      pattern: server-rules|serverRulesSections
---

<objective>
Add a public server rules page at /rules with static sectioned content, linked from header and footer, so users can view server rules (e.g. no cheating, be respectful) in an explanatory format.

Purpose: Satisfy RULES-01; single route, no auth, static content only.
Output: app/data/server-rules.ts, app/routes/rules._index.tsx, header/footer links, translation key HeaderRulesLabel, and a route/page test.
</objective>

<execution_context>
@C:/Users/Merih/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Merih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-server-rules/2-CONTEXT.md
@.planning/phases/02-server-rules/2-RESEARCH.md

# Route and nav patterns to follow
@app/routes/settings._index.tsx
@app/components/header.tsx
@app/components/footer.tsx
@app/root-meta.ts
</context>

<interfaces>
<!-- From app/root-meta.ts -->
getMetaTitle(key?: string) → meta config for document title

<!-- From app/routes/settings._index.tsx -->
export const meta = getMetaTitle("HeaderSettingsLabel");
export async function loader({ request }: Route.LoaderArgs) {
  await middleware(request);
  return null;
}

<!-- From app/components/header.tsx -->
HeaderLink({ to, icon, label, onClick?, disabled?, disabledText? })
Nav links use translate("HeaderSettingsLabel") etc.; add Rules link in same nav (e.g. with Settings in the right-side group).

<!-- Footer -->
Footer has copyright block; add a Rules link (e.g. next to or below copyright). Use Link from react-router or <a href="/rules">. Use translate("HeaderRulesLabel") for link text if translations are used in footer, else "Rules" or "Server Rules" per CONTEXT.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Static server rules data</name>
  <files>app/data/server-rules.ts</files>
  <action>
Create app/data/server-rules.ts with a typed array of sections. Each section has id (string), title (string), and body (string). Use 2–4 placeholder sections with explanatory copy (e.g. Behavior, No cheating, Respect, Consequences). Content is static only — no DB, no API. Match the pattern of app/data/backgrounds.ts (export const). Use a type or interface for the section shape and export it so the route can import. Per CONTEXT: sections grouped by category, explanatory tone.
  </action>
  <verify>
    <automated>File exists; tsconfig/build accepts app/data/server-rules.ts (e.g. npx tsc --noEmit 2>&1 | head -5)</automated>
  </verify>
  <done>server-rules.ts exports sections array and section type; sections have title and body with placeholder explanatory text.</done>
</task>

<task type="auto">
  <name>Task 2: Rules route and page</name>
  <files>app/routes/rules._index.tsx</files>
  <action>
Create app/routes/rules._index.tsx. Export meta = getMetaTitle("HeaderRulesLabel"). Export loader that calls middleware(request) and returns null. Default export: page component that imports server rules from ~/data/server-rules and renders sections (e.g. heading per section title, prose for body). Use Tailwind to match existing app (e.g. container, spacing, typography). Add a test file app/routes/rules._index.test.tsx (or rules.test.tsx next to route) that renders the route component (or loads /rules) and asserts at least one section title or body text is present, so RULES-01 is covered. Follow app/routes/settings._index.tsx for meta/loader pattern; use type Route from ./+types/rules._index.
  </action>
  <verify>
    <automated>npm test -- app/routes/rules --run</automated>
  </verify>
  <done>GET /rules renders page with sectioned rules; test passes; document title uses HeaderRulesLabel.</done>
</task>

<task type="auto">
  <name>Task 3: Header and footer links plus translation</name>
  <files>app/components/header.tsx, app/components/footer.tsx, app/translations/english.ts (and other locale files that define HeaderSettingsLabel)</files>
  <action>
Add HeaderRulesLabel to app/translations/english.ts (value e.g. "Rules" or "Server Rules"). Add the same key to every other translation file that defines HeaderSettingsLabel (grep for HeaderSettingsLabel in app/translations to get the list) so all locales show a translated label. In header.tsx add a HeaderLink: to="/rules", label={translate("HeaderRulesLabel")}, icon (e.g. faScroll or faClipboardList from FontAwesome), onClick={closeMenu} where appropriate — place it in the nav next to Settings (same desktop/mobile group). In footer.tsx add a link to /rules (e.g. "Rules" or use translate("HeaderRulesLabel")) in the footer block (e.g. after copyright or in the same div); use Link from react-router or plain <a href="/rules">. Per CONTEXT: both header and footer must link to /rules.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -20 (full suite); grep -l "HeaderRulesLabel" app/translations/*.ts | wc -l (keys added); grep -n "/rules" app/components/header.tsx app/components/footer.tsx</automated>
  </verify>
  <done>Header and footer each contain a link to /rules; HeaderRulesLabel present in english and all locales that have HeaderSettingsLabel.</done>
</task>

</tasks>

<verification>
- Navigate to /rules: page shows sectioned server rules content.
- Click "Rules" (or localized label) in header: navigates to /rules.
- Click "Rules" in footer: navigates to /rules.
- Document title reflects rules page (getMetaTitle).
</verification>

<success_criteria>
- RULES-01 satisfied: user can view a public server rules page with rules for players (sectioned, explanatory).
- Route /rules exists, public, no auth.
- Header and footer both link to /rules.
- At least one automated test covers the rules page content or route.
</success_criteria>

<output>
After completion, create .planning/phases/02-server-rules/02-01-SUMMARY.md
</output>
