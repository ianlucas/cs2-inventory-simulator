---
phase: 04-buy-vip
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/vip-packages.ts
  - app/env.server.ts
  - app/routes/vip._index.tsx
  - app/components/vip-page-content.tsx
  - app/components/header.tsx
  - app/components/footer.tsx
  - app/translations/english.ts
  - app/translations/turkish.ts
autonomous: true
requirements: [VIP-01, VIP-02]
user_setup: []

must_haves:
  truths:
    - "User can open /vip and see VIP information and 1/3/6 month package prices (TRY)"
    - "User can click Bynogame link and open it in a new tab (external URL from env)"
  artifacts:
    - path: app/data/vip-packages.ts
      provides: "Static packages: id, label, durationMonths, priceTry; export type and array"
    - path: app/routes/vip._index.tsx
      provides: "GET /vip route with loader returning packages and bynogameUrl; meta; default export page"
    - path: app/components/vip-page-content.tsx
      provides: "VIP benefits intro and package cards with price and CTAs (Bynogame link, PayTR placeholder for 04-03)"
  key_links:
    - from: app/routes/vip._index.tsx
      to: app/data/vip-packages.ts
      via: "Import packages; loader returns { packages, bynogameUrl }"
    - from: app/components/vip-page-content.tsx
      to: BYNOGAME_VIP_URL
      via: "Link with target=_blank when bynogameUrl is present"
      pattern: "target=\"_blank\"|rel=\"noopener\""
---

<objective>
Add VIP static data (1/3/6 month packages, TRY prices), BYNOGAME_VIP_URL env, /vip page with loader and content component, and header + footer links. Page shows VIP info, packages, and Bynogame external link; PayTR iframe will be added in 04-03.
</objective>

<execution_context>
@C:/Users/Merih/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Merih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-buy-vip/4-CONTEXT.md
@.planning/phases/04-buy-vip/4-RESEARCH.md

# Patterns
- rules._index.tsx: meta = getMetaTitle("HeaderRulesLabel"), loader with middleware(request), default export uses RulesPageContent with sections from server-rules.
- Header: HeaderLink to="/rules", icon={faScroll}, label={translate("HeaderRulesLabel")}, onClick={closeMenu}. Add VIP link in same nav group (next to Rules).
- Footer: Link to="/rules" with translate("HeaderRulesLabel"); add Link to="/vip" with translate("HeaderVipLabel") for consistency.
- Env: app/env.server.ts uses process.env.X ?? undefined for optional vars (see MYSQL_URL). Add BYNOGAME_VIP_URL the same way.
</context>

<tasks>

<task type="auto">
  <name>Task 1: VIP packages data and Bynogame env</name>
  <files>app/data/vip-packages.ts, app/env.server.ts, .env.example</files>
  <action>
Create app/data/vip-packages.ts. Export interface VipPackage { id: string; label: string; durationMonths: number; priceTry: number } (or equivalent). Export array of three packages: 1 month, 3 months, 6 months. Use Turkish labels (e.g. "1 Ay", "3 Ay", "6 Ay") and prices in TRY as numbers (e.g. 99, 249, 449 — planner may use placeholder values; CONTEXT says static file for prices). Optionally export a short vipBenefitsText string for the intro. Follow app/data/server-rules.ts pattern (typed export).

In app/env.server.ts add BYNOGAME_VIP_URL = process.env.BYNOGAME_VIP_URL ?? undefined (optional). In .env.example add a commented line: # BYNOGAME_VIP_URL=https://... (Bynogame VIP purchase link on Buy VIP page).
  </action>
  <verify>
    <automated>test -f app/data/vip-packages.ts && grep -q "durationMonths\|priceTry" app/data/vip-packages.ts && grep -q BYNOGAME_VIP_URL app/env.server.ts</automated>
  </verify>
  <done>vip-packages.ts exports packages (1/3/6 month, TRY); BYNOGAME_VIP_URL in env and .env.example.</done>
</task>

<task type="auto">
  <name>Task 2: /vip route and VIP page content component</name>
  <files>app/routes/vip._index.tsx, app/components/vip-page-content.tsx</files>
  <action>
Create app/routes/vip._index.tsx. Export meta = getMetaTitle("HeaderVipLabel") (translation key to be added in Task 3). Loader: await middleware(request); import packages from ~/data/vip-packages; import BYNOGAME_VIP_URL from ~/env.server; return { packages, bynogameUrl: BYNOGAME_VIP_URL ?? null }. Default export: page component that uses useLoaderData() and renders VipPageContent with packages and bynogameUrl. Use type Route from ./+types/vip._index.

Create app/components/vip-page-content.tsx. Props: { packages: VipPackage[]; bynogameUrl: string | null }. Render: (1) Page title/heading (e.g. "VIP Satın Al" or "Buy VIP"). (2) Short intro/benefits text (Turkish; can come from vip-packages or hardcoded). (3) For each package: card/section with label, duration, price in TRY (e.g. "99 ₺"), and two CTAs: (a) "Bynogame'da al" — only render if bynogameUrl is set; use <a href={bynogameUrl} target="_blank" rel="noopener noreferrer">. (b) "PayTR ile öde" (or similar) — for now a button or placeholder that will be wired to PayTR iframe in 04-03 (e.g. disabled or "Yakında"). Use Tailwind; match rules page card style (rounded border, bg-stone-900/80). Turkish only per CONTEXT.
  </action>
  <verify>
    <automated>grep -q "vip._index" app/routes/vip._index.tsx 2>/dev/null; grep -q "VipPageContent\|vip-page-content" app/routes/vip._index.tsx && grep -q "target=\"_blank\"" app/components/vip-page-content.tsx</automated>
  </verify>
  <done>GET /vip shows VIP info and three packages with prices; Bynogame link opens in new tab when env set.</done>
</task>

<task type="auto">
  <name>Task 3: Header and footer link to /vip; translation key</name>
  <files>app/components/header.tsx, app/components/footer.tsx, app/translations/english.ts, app/translations/turkish.ts</files>
  <action>
In app/components/header.tsx add HeaderLink to="/vip" with icon (e.g. faCrown or faStar from FontAwesome), label={translate("HeaderVipLabel")}, onClick={closeMenu}. Place it in the same nav group as Rules (both signed-in and signed-out branches — see existing Rules HeaderLink placement).

In app/components/footer.tsx add a link to /vip: after the Rules link, add separator " · " and <Link to="/vip"> with translate("HeaderVipLabel"). Match the pattern of the Rules link (underline hover:text-white).

Add HeaderVipLabel to app/translations/english.ts (e.g. "VIP" or "Buy VIP"). Add HeaderVipLabel to app/translations/turkish.ts (e.g. "VIP" or "Satın Al"). Add the same key to all other locale files that define HeaderRulesLabel (list from grep HeaderRulesLabel app/translations/*.ts) so the nav shows a translated label in every locale; value can mirror English "VIP" where no translation is needed.
  </action>
  <verify>
    <automated>grep -q 'to="/vip"' app/components/header.tsx && grep -q 'to="/vip"' app/components/footer.tsx && grep -q HeaderVipLabel app/translations/english.ts && grep -q HeaderVipLabel app/translations/turkish.ts</automated>
  </verify>
  <done>Header and footer each have a link to /vip; HeaderVipLabel exists in english, turkish, and other locales that have HeaderRulesLabel.</done>
</task>

</tasks>

<verification>
- Visit /vip: page loads with packages and prices in TRY; when BYNOGAME_VIP_URL is set, Bynogame link is visible and opens in new tab.
- Header and footer show VIP link; translation key used for label.
</verification>

<success_criteria>
- VIP-01: User can view Buy VIP page with VIP information and prices.
- VIP-02: User can open Bynogame purchase via external link from Buy VIP page.
</success_criteria>

<output>
After completion, create .planning/phases/04-buy-vip/04-02-SUMMARY.md
</output>
