---
phase: 03-homepage
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - app/routes/api.servers.players._index.tsx
  - app/routes/_index.tsx
  - app/components/server-card.tsx
autonomous: true
requirements: [HOME-04]
user_setup: []

must_haves:
  truths:
    - "User can click a server card and see a modal with the current player list"
    - "Modal shows player names (with fallback for empty); optional score/duration from raw"
    - "Modal handles loading and empty states"
  artifacts:
    - path: app/routes/api.servers.players._index.tsx
      provides: "Resource route GET returning { players } for host+port (query params)"
    - path: app/routes/_index.tsx
      provides: "Selected server state, Modal, useFetcher for players"
  key_links:
    - from: app/routes/_index.tsx
      to: app/routes/api.servers.players._index.tsx
      via: "useFetcher().load('/api/servers/players?host=...&port=...')"
      pattern: "fetcher.load|useFetcher"
    - from: app/routes/api.servers.players._index.tsx
      to: gamedig
      via: "GameDig.query type counterstrike2 for single server"
      pattern: "counterstrike2"
---

<objective>
Enable opening a modal with the current player list when the user clicks a server card. Data comes from a resource route that runs gamedig for that server; modal reuses Modal + ModalHeader, shows player names (with fallback for empty), optional raw fields, and handles loading/empty states.
</objective>

<execution_context>
@C:/Users/Merih/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Merih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/3-CONTEXT.md
@.planning/phases/03-homepage/3-RESEARCH.md

# From Plan 01–02
- Index loader returns { servers }; gamedig runs server-side only.
- ServerCard has optional onSelect(server); index page has grid/table.
# Reuse
- Modal, ModalHeader from app/components/modal.tsx — hidden prop, onClose, title; ModalHeader needs onClose or linkTo for close.
- CONTEXT: players from gamedig state.players: { name, raw }[]; name often empty in CS2 — show "—" or "Unknown".
</context>

<interfaces>
From app/components/modal.tsx:
- Modal({ hidden, blur, children, className?, fixed? })
- ModalHeader({ title, onClose?: () => void, linkTo?: string })
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Resource route for players</name>
  <files>app/routes/api.servers.players._index.tsx</files>
  <action>
Create app/routes/api.servers.players._index.tsx. Loader: read host and port from request URL searchParams (e.g. request.url and new URL(request.url).searchParams.get("host"), get("port")). Validate presence; if missing return 400 or empty players. Run gamedig.query({ type: "counterstrike2", host, port: Number(port) }) in the loader. Return JSON response { players: state.players } (array of { name, raw }). Use middleware(request) at start of loader. Follow project API route pattern (see app/routes/api.user.$userId._index.tsx for loader shape); this route returns JSON so use Response.json({ players }). Handle errors: if query throws, return { players: [] } or 503 so the modal can show empty/error. Do not expose gamedig to client; keep query in this server route only.
  </action>
  <verify>
    <automated>grep -q "counterstrike2" app/routes/api.servers.players._index.tsx; grep -q "searchParams\|get.*host\|get.*port" app/routes/api.servers.players._index.tsx</automated>
  </verify>
  <done>GET /api/servers/players?host=...&port=... returns { players: [...] } from gamedig for that server.</done>
</task>

<task type="auto">
  <name>Task 2: Player modal on card click</name>
  <files>app/routes/_index.tsx, app/components/server-card.tsx</files>
  <action>
In app/routes/_index.tsx: (1) Add state for selected server (host, port) or null when modal closed. (2) Pass onSelect(server) to each ServerCard that sets selected server (and opens modal). (3) When selectedServer is set, render Modal (hidden={!selectedServer}), ModalHeader title="Players" (or similar) with onClose that clears selectedServer. (4) Use useFetcher(); when selectedServer changes and is non-null, call fetcher.load(`/api/servers/players?host=${encodeURIComponent(selectedServer.host)}&port=${selectedServer.port}`). (5) In modal body: if fetcher.state === "loading" show loading message; if fetcher.data?.players show list — each player: display name (player.name || "—" or "Unknown"); optionally show score/duration from player.raw if available and useful. Sort players by name (or by score if preferred). If fetcher.data?.players?.length === 0 show "No players" or empty state. (6) Ensure ServerCard accepts onSelect and is clickable when onSelect is provided (if not already from Plan 02). Close modal on overlay/onClose only; do not close on fetch complete.
  </action>
  <verify>
    <automated>npm test -- --run app/routes/_index app/components/server-card 2>/dev/null || true; grep -q "Modal\|ModalHeader" app/routes/_index.tsx && grep -q "useFetcher\|fetcher.load" app/routes/_index.tsx</automated>
  </verify>
  <done>Clicking a server card opens modal; modal shows loading then player list (names with fallback) or empty state; close button closes modal.</done>
</task>

</tasks>

<verification>
- Run phase-relevant tests. Manually: click card → modal opens, players load (or empty), close works.
</verification>

<success_criteria>
- Resource route returns players for a given host/port.
- Clicking a server card opens a modal that fetches and displays that server's player list (or empty/loading).
</success_criteria>

<output>
After completion, create .planning/phases/03-homepage/03-03-SUMMARY.md
</output>
