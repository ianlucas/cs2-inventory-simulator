---
phase: 03-homepage
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/components/server-card.tsx
  - app/routes/_index.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03]
user_setup: []

must_haves:
  truths:
    - "User sees server list on homepage as vertical cards with map background and server data"
    - "User can switch between grid and table display; default is grid"
  artifacts:
    - path: app/components/server-card.tsx
      provides: "Vertical card with map background (getMapThumbnailUrl), server name, map, player count"
    - path: app/routes/_index.tsx
      provides: "Page that renders server list (grid or table), view toggle"
  key_links:
    - from: app/components/server-card.tsx
      to: app/data/map-thumbnails.ts
      via: "getMapThumbnailUrl(server.map)"
      pattern: "getMapThumbnailUrl"
    - from: app/routes/_index.tsx
      to: loader data
      via: "useLoaderData for servers array"
      pattern: "useLoaderData"
---

<objective>
Render the homepage server list as vertical cards with map background (fixed CS2 map thumbnails) and server data; add grid/table toggle with default grid. Reuse rules-page card styling (rounded-xl, border, bg-stone-900/80) for consistency.
</objective>

<execution_context>
@C:/Users/Merih/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Merih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/3-CONTEXT.md
@.planning/phases/03-homepage/3-RESEARCH.md

# From Plan 01
- Index loader returns { servers } (gamedig state or offline); type from _index.tsx or shared type
- getMapThumbnailUrl(mapName) from app/data/map-thumbnails.ts
- Modal, ModalHeader in app/components/modal.tsx (for Plan 03)
# Existing
- app/components/rules-page-content.tsx: rounded-xl border border-stone-600/50 bg-stone-900/80 shadow-lg backdrop-blur-sm
- Tailwind dark theme (stone/neutral, white text)
</context>

<interfaces>
Server item from loader: at least { name?, map?, numplayers?, maxplayers?, connect?, ping?, offline? }. Card receives one server object and optional onSelect(server) for Plan 03 (click to open modal).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Server card component</name>
  <files>app/components/server-card.tsx</files>
  <action>
Create app/components/server-card.tsx. Props: server (object with name, map, numplayers, maxplayers, connect, ping; or offline flag), optional onSelect?: (server) => void for click handler. Render a vertical card: background image from getMapThumbnailUrl(server.map) (or fallback when offline/unknown map), overlay with server name, map name, player count (e.g. "numplayers / maxplayers"). Style per rules page: rounded-xl, border, bg-stone-900/80 or similar overlay so text is readable; min height so card is visibly vertical. If onSelect provided, make card clickable (button or clickable div with cursor-pointer); otherwise presentational only. Handle offline servers: show "Offline" or placeholder text and optional host/port. Use existing Tailwind and dark theme.
  </action>
  <verify>
    <automated>npm test -- --run app/components/server-card 2>/dev/null || true; test -f app/components/server-card.tsx && grep -q getMapThumbnailUrl app/components/server-card.tsx</automated>
  </verify>
  <done>ServerCard renders map background and server data; accepts onSelect for Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: Index page with grid/table toggle</name>
  <files>app/routes/_index.tsx</files>
  <action>
In app/routes/_index.tsx default export: useLoaderData to get { servers }. Add client state for view mode: "grid" | "table", default "grid". Optional: persist in localStorage (planner discretion). Render: (1) A view toggle (e.g. two buttons or segmented control: Grid / Table). (2) When grid: a responsive grid of ServerCard components (e.g. grid-cols-1 md:grid-cols-2 lg:grid-cols-3), one card per server. (3) When table: same server data in a table (rows: server name, map, players, connect/ping; headers as needed). Use ServerCard for grid; for table either reuse a compact row representation or plain table cells. Empty state: when servers is empty or all null, show a short message (e.g. "No servers" or "All servers offline"). Page title/heading appropriate for homepage (e.g. "Servers" or from meta). Do not add modal or player fetch yet (Plan 03); card can have onSelect no-op or omit until Plan 03.
  </action>
  <verify>
    <automated>npm test -- --run app/routes/_index 2>/dev/null || true; grep -q "grid\|table" app/routes/_index.tsx && grep -q "ServerCard\|server-card" app/routes/_index.tsx</automated>
  </verify>
  <done>Homepage shows server list as grid (default) or table; toggle switches layout; empty state handled.</done>
</task>

</tasks>

<verification>
- Run phase-relevant tests. Confirm grid is default and toggle changes layout.
</verification>

<success_criteria>
- Server list displays as vertical cards with map background and server data.
- User can toggle between grid and table; default is grid.
</success_criteria>

<output>
After completion, create .planning/phases/03-homepage/03-02-SUMMARY.md
</output>
