---
phase: 03-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/servers.ts
  - app/data/map-thumbnails.ts
  - app/components/hooks/use-root-layout.ts
  - app/routes/_index.tsx
  - package.json
autonomous: true
requirements: [HOME-01]
user_setup: []

must_haves:
  truths:
    - "Homepage loader returns server list from gamedig (CS2)"
    - "Server list source is repo config; map thumbnails have fallback"
    - "Root path (/) hides inventory so server list is main content"
  artifacts:
    - path: app/data/servers.ts
      provides: "List of servers to query (host, port)"
    - path: app/data/map-thumbnails.ts
      provides: "Map name → thumbnail URL + getMapThumbnailUrl with fallback"
    - path: app/routes/_index.tsx
      provides: "Loader that runs gamedig per server, returns { servers }"
  key_links:
    - from: app/routes/_index.tsx
      to: gamedig
      via: "GameDig.query({ type: 'counterstrike2', host, port })"
      pattern: "counterstrike2"
    - from: app/routes/_index.tsx
      to: app/data/servers.ts
      via: "Import and iterate"
      pattern: "servers"
---

<objective>
Provide data foundation and index loader for the homepage server list. Server list comes from a repo config file; map thumbnails from a data module with fallback; index route loader runs gamedig (CS2) per server and returns results; root path hides inventory so server list is the main content at /.
</objective>

<execution_context>
@C:/Users/Merih/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Merih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/3-CONTEXT.md
@.planning/phases/03-homepage/3-RESEARCH.md

# Existing patterns
- Loader: app/routes/_index.tsx uses Route.LoaderArgs, middleware(request), returns null
- useRootLayout: app/components/hooks/use-root-layout.ts has ROUTES_WITHOUT_INVENTORY = ["/rules"]; add "/" so pathname === "/" hides inventory
- Data modules: app/data/backgrounds.ts and app/data/server-rules.ts export typed data; map-thumbnails same pattern (map name → URL)
- Gamedig: server-side only (loaders or resource routes); type "counterstrike2" for CS2; use Promise.allSettled so one offline server does not break the list
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server config and map thumbnails data</name>
  <files>app/data/servers.ts, app/data/map-thumbnails.ts, package.json</files>
  <action>
Create app/data/servers.ts exporting a list of servers to query. Each entry: { host: string, port?: number } (port optional, gamedig uses game default if omitted). Use TypeScript (same pattern as app/data/server-rules.ts). Example: export const SERVER_LIST = [{ host: "127.0.0.1", port: 27015 }, ...].

Create app/data/map-thumbnails.ts: a Record or map of map name (string) → thumbnail URL (string), plus a fallback URL for unknown maps. Export getMapThumbnailUrl(mapName: string): string that returns MAP_THUMBNAILS[mapName] ?? FALLBACK_URL. Use same pattern as app/data/backgrounds.ts (fixed set). Include at least a few common CS2 map names (e.g. de_dust2, de_mirage) and a fallback; fallback can be a placeholder path (e.g. /images/map-fallback.png) or in-repo asset.

Install gamedig: npm install gamedig. Do not import gamedig in any client bundle; use only in loaders or API routes.
  </action>
  <verify>
    <automated>test -f app/data/servers.ts && test -f app/data/map-thumbnails.ts && grep -q getMapThumbnailUrl app/data/map-thumbnails.ts && grep -q gamedig package.json</automated>
  </verify>
  <done>servers.ts and map-thumbnails.ts exist and export SERVER_LIST (or equivalent) and getMapThumbnailUrl; gamedig in package.json dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Hide inventory on homepage</name>
  <files>app/components/hooks/use-root-layout.ts</files>
  <action>
In app/components/hooks/use-root-layout.ts, add "/" to ROUTES_WITHOUT_INVENTORY so that when pathname === "/" the inventory is hidden. Change from ROUTES_WITHOUT_INVENTORY = ["/rules"] to include "/" (e.g. ["/", "/rules"]). Existing logic uses pathname === path || pathname.startsWith(path + "/"), so "/" will match the index route.
  </action>
  <verify>
    <automated>grep -q '"/"' app/components/hooks/use-root-layout.ts && grep -q "ROUTES_WITHOUT_INVENTORY" app/components/hooks/use-root-layout.ts</automated>
  </verify>
  <done>Visiting / returns inventory: false from useRootLayout; header + outlet + footer show without inventory.</done>
</task>

<task type="auto">
  <name>Task 3: Index loader with gamedig</name>
  <files>app/routes/_index.tsx</files>
  <action>
In app/routes/_index.tsx loader: (1) await middleware(request). (2) Import server list from app/data/servers (e.g. SERVER_LIST). (3) Import GameDig from "gamedig", create instance, then Promise.allSettled(serverList.map(s => gamedig.query({ type: "counterstrike2", host: s.host, port: s.port ?? 27015 }))). (4) Map results: fulfilled → value (gamedig state: name, map, numplayers, maxplayers, players, connect, ping, etc.); rejected → null or { offline: true, host, port }. (5) Return { servers: [...] } so the page can consume via useLoaderData. Export a type for the server item (e.g. ServerState or use gamedig's return type) so Plan 02 can type the UI. Do not run gamedig in the browser; keep all gamedig imports and calls inside the loader.
  </action>
  <verify>
    <automated>npm test -- --run app/routes/_index 2>/dev/null || true; grep -q "counterstrike2" app/routes/_index.tsx && grep -q "allSettled\|Promise.allSettled" app/routes/_index.tsx</automated>
  </verify>
  <done>GET / returns loader data { servers }; each entry is gamedig state or offline placeholder; no gamedig in client bundle.</done>
</task>

</tasks>

<verification>
- Run npm test -- --run (phase-relevant tests when present).
- Confirm gamedig only in server-side code (loader); no import in client-only files.
</verification>

<success_criteria>
- Server list config and map thumbnails modules exist; gamedig installed.
- Root path hides inventory; index loader returns server list from gamedig (or offline placeholders).
</success_criteria>

<output>
After completion, create .planning/phases/03-homepage/03-01-SUMMARY.md
</output>
